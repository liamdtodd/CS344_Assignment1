# CS344_Assignment1

COMPILE:
To compile type 'make' into the command line

EXECUTION:
To execute the code, type 'a.out' into the command line.
Additionally, 'valgrind a.out' will show the memory leak report
